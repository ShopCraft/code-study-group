# Test Import 

import colors

print("This is my test file.\n")
print(colors.Teal) 

print("Blue is", colors.Blue) # This works

print("Teal is") 
#colors.get_colorCode('Teal') # This does not work 
# figure out import later
#colors.get_colorCode(colors.Teal)) # This does not work 

#print("Teal is",colors.get_colorCode(colors.Teal)) # This doesn't work 
print("Teal is") 
#colors.get_colorCode(colors.Teal)) # This does work 
